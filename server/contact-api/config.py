"""Настройки приёмника: всё из окружения, значений по умолчанию для секретов нет.

Секреты — токен бота и пароль SMTP — по-прежнему только из `.env` на сервере.
А вот получатель заявок секретом быть перестал: с 04.09.2026 это корпоративная
`info@jw-dev.pro`, она напечатана на самом сайте и прячется ровно ни от кого.
Личного ящика владельца здесь больше нет — по его решению он не участвует
в работе сайта вообще.

Поэтому у `MAIL_TO` появилось значение по умолчанию. Адрес продублирован
из `src/data/site.ts` — связать константу питона с константой TypeScript нечем,
а проверки на их расхождение нет ни одной: сайт и приёмник живут в разных
языках и катятся разными путями. Меняешь адрес — меняй в обоих местах.

Пустая строка в `.env` теперь означает «шли на корпоративную», а не «почта
не настроена»: за то, включена ли почта вообще, отвечает `SMTP_HOST`.
"""

import os

BOT_TOKEN = os.environ["TELEGRAM_BOT_TOKEN"]
CHAT_ID = os.environ["TELEGRAM_CHAT_ID"]

# Дубль на почту необязателен: пока SMTP не настроен, заявка идёт только в Telegram.
SMTP_HOST = os.environ.get("SMTP_HOST", "")
SMTP_PORT = int(os.environ.get("SMTP_PORT", "587"))
SMTP_USER = os.environ.get("SMTP_USER", "")
SMTP_PASSWORD = os.environ.get("SMTP_PASSWORD", "")

# Совпадает с SITE.email в src/data/site.ts. Переопределяется через .env,
# но переопределять больше нечем: другого ящика у сайта нет.
MAIL_TO = os.environ.get("MAIL_TO") or "info@jw-dev.pro"

RATE_LIMIT_PER_HOUR = int(os.environ.get("RATE_LIMIT_PER_HOUR", "5"))

# Человек не заполняет форму быстрее этого. Проверка работает только когда
# у заявки есть отметка времени — её ставит скрипт, без JS её нет.
MIN_FILL_SECONDS = 3

PORT = int(os.environ.get("PORT", "8080"))
