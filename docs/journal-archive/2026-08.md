# Журнал разработки — август 2026

Записи, уехавшие из `JOURNAL.md` по лимиту в 200 строк. Новое сверху.

---

## 2026-08-28 · Бюджеты слоёв: ворота нашли слой, который держался зря

- Сделано: `check:layers` в `npm run check`, восемнадцать проверок на сами ворота.
  Три правила: несепарабельные режимы смешивания запрещены, `will-change` стоит только
  там, где что-то действительно движется, `@keyframes` анимирует только дешёвое.
  Пункт 4 долга закрыт, а с ним и весь долг на ворота производительности — все пять пунктов.
- Находка: `.ring-in` в кольце стека держал `will-change: transform`, хотя трансформ ему
  никто не пишет — `ring.js` расставляет каждый чип отдельно. Постоянный слой композитора
  за ничто. Снято, `preserve-3d` на месте: 3D-контекст чипам нужен, обещание движения — нет.
- Приёмка не на слово: прогон по дереву `9b2b0f9^` находит пульс живой точки, который
  анимировался `box-shadow`, — ровно то место, что ревизия 26.08 нашла руками и переписала
  на кольцо с `transform`/`opacity`. По `HEAD` ворота молчат.
- Решение: посылку плана «парный сброс для каждого `will-change`» пришлось поправить,
  как и в прошлый раз. Сбрасывать бесконечный дрейф пятен нечего — правило пришлось бы
  глушить исключениями. Проверяется причина, а не сброс, и для покадровых элементов она
  выводится из `FRAME_TARGETS` у `check:frames`: один список на два ворот, устареть врозь
  они не могут.
- Решение: потолок на число режимов смешивания — сегодняшнее число, пять, без запаса.
  Это храповик, а не замер: новый слой смешивания должен стоить осознанного решения,
  а не появляться между делом.
- Грабли: `stripComments` выбрасывал комментарий вместе с его переводами строк, и номера
  строк в отчёте уезжали тем сильнее, чем длиннее комментарий выше. Правка на один символ,
  но врали все ворота, которые ходят через разбор CSS.

## 2026-08-28 · Ворота, которые прокручивают, — и сразу находка

- Сделано: `check:runtime` в `npm run verify`. Шесть секунд свайпа по главной на
  замедленном вчетверо процессоре, телефонный экран, три прогона. Снимает две величины:
  обращения к геометрии за прокрутку и длительность кадра. Приёмка не на слово: по дереву
  `9b2b0f9^` выходит p95 = 66,7 мс и 567 обращений, по `HEAD` — 50 мс и 220. Пункты 2 и 3
  долга закрыты одним прогоном, как и планировалось.
- Сделано: `check:browsers` наконец открывает контекст с касанием и двойной плотностью
  растра — мобильную ветку CSS в CI не проверял никто. Пункт 5 долга. Ворота теперь сами
  проверяют, что контекст мобильный: без этой строки правка непроверяема — они зеленели
  и с настольным контекстом.
- Находка: ворота, едва включённые, нашли то, ради чего писались. Медиана кадра здорова
  везде — 16,7 мс. Рвётся хвост: p90 = 33 мс на обычном свайпе и 50 мс на резком флике,
  при том что `/ru/privacy/` в тех же условиях даёт 17,3 мс и ноль длинных кадров. Значит
  общий слой сайта здоров, дорога начинка главной. Записано отдельным пунктом долга.
- Решение: пороги — сегодняшний замер плюс запас, а не идеал. `frameP95` в 60 мс выглядит
  громадным и держит ровно то, что есть сейчас. Опускать его надо вместе с починкой:
  порог, поставленный по мечте, красит CI на коде, который никто не менял.
- Грабли: первый порог я выдумал — поставил 20 обращений к геометрии, «в идеале должно
  быть ноль». Их сто, и они законны: граф перезамеряет якоря раз в 1120 мс по гайду.
  Правило «цифры только измеренные» относится и к порогам, а не только к витрине.
- Грабли: ни одна гипотеза о причине рывков не подтвердилась — ни канвас, ни стекло,
  ни блендинг, ни появление блоков, ни плотность растра. Выяснилось это за час
  отключений с замером после каждого; неделя догадок стоила бы дороже.
  Дальше: пункт 4 долга — статические бюджеты слоёв.

## 2026-08-27 · Форма заговорила: бот заявок и немой контейнер

- Сделано: владелец завёл бота у @BotFather, `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
  легли в `.env` на сервере, контейнер пересоздан. Заявка с прода проверена сквозным
  запросом на `https://jw-dev.pro/api/contact` — доходит. Главный блокер Ф12 снят.
- Решение: токен не проходил через чат и историю команд — владелец вписал его сам
  по ssh. Проверка доставки этого и не требует: единственный путь к `ok: true` мимо
  лимита и ловушки лежит через `raise_for_status()` на ответе Telegram, так что
  доставка доказывается публичным запросом, а секрет остаётся в контейнере.
- Грабли: `contact-api` не написал в `docker logs` ни строки за 38 часов, включая
  стартовый баннер. `CMD ["python", "app.py"]` без `PYTHONUNBUFFERED`: когда stdout
  не терминал, Python буферизует его блоками, и `log.exception("заявка не доставлена
в Telegram")` копился бы в памяти процесса — молчал бы ровно тот журнал, ради
  которого в журнал и смотрят. Одна строка `ENV` в `Dockerfile`, пересборка на проде.
- Грабли: на пустых логах я чуть не построил доказательство доставки — «ошибок нет,
  значит дошло». Проверять надо было сам канал: после починки заявка с ловушкой
  печатает свою строку, и только теперь тишина в логе что-то значит.

## 2026-08-26 · Ворота на эффект, спорящий сам с собой

- Сделано: `check:frames` в `npm run check` и `check:gates` — двенадцать проверок на сами
  ворота. Приёмка не на слово: прогон по дереву `9b2b0f9^` находит ровно те три конфликта,
  что ревизия нашла руками, — `.hdr`, `.dock-p .fg`, `.prail i` — и двенадцать голых
  `transition` сверх них; по `HEAD` молчит.
- Решение: правило «`transition` обязан называть свойства» — сверх плана и оказалось самым
  ценным. `transition: .4s var(--e)` означает `all`: браузер берётся интерполировать что
  угодно, включая покадровое и дорогое. Так под переход попало размытие шапки.
- Решение: область действия задаёт карта `FRAME_TARGETS`, а не имя файла. Скрипт и стили
  адресуют один элемент по-разному: `dock.js` берёт кольцо как `#dock-arc`, а `dock.css`
  красит его как `.dock-p .fg`. Пара по имени файла заодно врала бы: в `process.css` рядом
  с покадровой точкой рельсы законно живёт переход `transform` на карточке шага.
- Решение: у ворот появились свои тесты. Разбор регулярками ошибается молча — пропущенный
  случай выглядит как зелёные ворота, а не как поломка. Часть проверок написана прямо
  на случаи, где наивная реализация промахивается.
- Грабли: посылка плана «собрать пересечение и всё» не работает. Голое пересечение имён
  свойств даёт `transform` и `opacity` из половины компонентов — ворота, которые надо
  глушить списком исключений, хуже отсутствующих.
- Осталось из долга четыре пункта из пяти, все рантайм: счётчик форсированных layout,
  бюджет кадра под прокруткой, статические бюджеты слоёв и `check:browsers` без
  `hasTouch`. Дальше: рантайм-ворота — они закрывают пункты 2 и 3 одним прогоном.

## 2026-08-26 · Бюджет кадра — почему сайт лагал на телефоне

- Сделано: замер вместо догадок. Шесть секунд прокрутки главной при троттлинге CPU ×4
  давали медиану кадра 67,8 мс — пятнадцать кадров в секунду вместо шестидесяти. После
  правок 17,0 мс на десктопе и 17,5 мс p95 на мобильном профиле, длинных кадров было 66,
  стало 11. Вес тут ни при чём: главная и до, и после уходит в 52 КБ с gzip при бюджете 260.
- Решение: растр графа один к одному с CSS-пикселями вместо двойного. Канвас растянут
  во весь экран, и удвоение плотности учетверяет площадь заливки — одна эта строка стоила
  67,8 → 17,0 мс. Сеть рисуется линиями в один пиксель под непрозрачностью .66, чёткости,
  за которую платят вчетверо, на них не видно. Отступление от §9 записано в гайде.
- Решение: на ≤900px блюр стекла заменён плотной заливкой. Рецепт не новый — §5 задаёт
  его сам для `html.noblur`. Под стеклом лежит канвас, и каждый его кадр требовал заново
  пересчитать размытие под шапкой, доком и тремя окнами героя.
- Грабли: граф перерождался посреди прокрутки. Мобильный браузер шлёт `resize` на каждое
  прятанье адресной строки, а обработчик звал полную перестройку поля — все узлы заново
  через `Math.random()`. Владелец описал это как «меняет кадры и форму». Теперь поле
  перестраивается только при смене ширины: от высоты не зависят ни число узлов, ни радиус.
- Грабли: эффект, спорящий сам с собой, встретился четырежды. Точка рельсы процесса,
  кольцо дока и радиус блюра шапки анимировались переходом по свойству, которое скрипт
  пишет каждый кадр, — переход не сглаживает его, а перезапускается шестьдесят раз
  в секунду. А чип кольца стека при наведении поднимался на 12px и уезжал из-под курсора,
  тут же терял наведение и падал обратно: зона наведения теперь с запасом снизу.
- Грабли: `prefers-reduced-motion` читался как поломка. `transition-duration:.01ms` на `*`
  гасил и появление блоков — страница открывалась разом собранной. Именно так владелец
  увидел её в Chrome на Android с системной настройкой «убрать анимации». Теперь гасится
  движение, а переходы прозрачности, цвета и акцента остаются.

## 2026-08-25 · Запуск — домен, сервер, HTTPS и автовыкат

- Сделано: сайт живёт на `https://jw-dev.pro`. Дроплет поднят с нуля — пользователь,
  своп, ufw, Docker; сертификат Let's Encrypt на оба имени, обновление через `webroot`
  проверено вхолостую; выкат идёт сам после зелёного push в `main`. Не проверена
  доставка заявки: нужен токен бота.
- Решение: certbot на хосте, а не отдельным контейнером. Обновление раз в три месяца
  не стоит второго образа при 453 МБ памяти, а `certbot.timer` в Ubuntu уже есть.
  Сертификаты и каталог подтверждения смонтированы в nginx только на чтение.
- Грабли: `types` на уровне server карту MIME-типов заменяет, а не дополняет. Одна
  строка про манифест стоила типов всему остальному — HTML уходил как
  `application/octet-stream`, то есть скачивался файлом вместо того, чтобы открыться.
- Грабли: `add_header` в location отменяет унаследованные заголовки, а не дополняет их.
  CSP не доезжал до HTML — до всего, ради чего в проекте запрещён инлайн. Заголовки
  вынесены в `snippets/` и подключаются `include` там, где есть свой `add_header`.
- Грабли: `location ~* \.html$` не срабатывал никогда — страницы адресуются каталогами,
  а `try_files` отдаёт `index.html`, не перевыбирая location, и `Cache-Control` стоял
  на одной главной. Заодно `^~` перед `/ru/` отрезал русскую половину сайта от правил
  для картинок, ленты и манифеста. Английская 404 не находилась: Astro кладёт её
  файлом `404.html`, а не каталогом.
- Грабли: Docker монтирует одиночный файл по inode, а rsync подменяет inode при записи.
  Правка выглядела выкаченной, `nginx -t` при этом проверял старую копию и отвечал
  «в порядке». Лечится `--inplace`, записано в `deploy/README.md`. Дальше: контентная
  половина Ф12.

## 2026-08-25 · Фаза 11 — качество, четверо ворот в браузере

- Сделано: axe на WCAG 2.2 AA по всем страницам, сценарии без JavaScript, три движка
  на узком экране и Lighthouse на мобильном. Всё в CI и в выкате: красное — деплоя нет.
- Найдено воротами: блок кода в статье прокручивается вбок, но фокус не берёт —
  мышью строку дочитать можно, клавишами нет. `tabindex="0"` ставит плагин Sätteri:
  разметку кода делает markdown, в шаблоне статьи этих узлов ещё нет.
- Решение: свой статический сервер вместо `astro preview`. Предпросмотр в Astro 7 —
  демон-одиночка: уже запущенный экземпляр отдал бы ту сборку, с которой поднялся.
  Наткнулись на это сразу — на порту висел четырёхчасовой сервер прошлой сессии.
- Решение: `lighthouse` напрямую, а не `@lhci/cli`. Свой отчёт того же вида, что
  у остальных ворот, и один сервер на все четыре проверки вместо второго конфига.
- Решение: правило `on*=` теперь ловит любой атрибут на «on», а не список имён.
  `ontoggle` и `onpointerdown` проходили мимо ворот, которые обязаны не находить ничего.
- Грабли: `rehypePlugins` в Astro 7 требуют вернуть старый обработчик markdown — по
  умолчанию стоит Sätteri со своими `hastPlugins`. Менять обработчик ради атрибута
  не стали. Дальше: Ф12 — реальный контент и запуск.

## 2026-08-25 · Фаза 10 — SEO и шаринг

- Сделано: карточки шаринга на всех типах страниц, граф schema.org, иконки для iOS
  и Android с манифестом, отложенная Метрика, настоящая политика и ворота `check:seo`.
- Решение: раскладка карточки одна на все страницы, содержание — в `og-cards.ts`.
  Путь картинки повторяет раздел сайта: плоское имя однажды столкнуло бы слаг кейса
  со слагом статьи.
- Решение: услуги размечены типом `Service`, а не `ProfessionalService` из плана.
  Последний — подтип `LocalBusiness`, то есть заявка на заведение с адресом, а гео
  на сайте нет по решению владельца от 25.08.2026. Отступление записано в плане.
- Решение: `favicon.ico` нет. PNG 32×32 закрывает те же случаи, а `.ico` sharp не пишет —
  пришлось бы держать в репозитории картинку, собранную вне сборки.
- Решение: счётчик грузится после первого действия человека или через 4 с простоя,
  вебвизор выключен, «не отслеживать» уважается модулем — Метрика эту настройку не читает.
- Решение: раздел политики про аналитику показывается только когда задан `PUBLIC_METRIKA_ID`.
  Без номера модуль счётчика вырезается сборщиком целиком — строки `yandex` в dist нет вовсе.
- Грабли: `inLanguage` у `Service` в словаре schema.org нет — сверено с schema.org/Service,
  свойство снято. Валидатор пометил бы замечанием то, что казалось безобидным дублем языка.
- Грабли: sharp рисует SVG в его собственном размере и только потом масштабирует. Без
  пересчёта `density` от стороны иконка 512 выходила растянутым квадратиком 64×64.
- Не проверено: превью в Telegram и онлайн-валидатор schema.org — то и другое требует
  живого домена. Локально проверены структура графа, ссылки `@id` и наличие файлов.
  Дальше: фаза 11 — качество.

## 2026-08-25 · Ревизия текстов — сцена продаёт услугу, а не чужой сайт

- Сделано: переписаны подзаголовок героя, панель сцены, три пилюли из четырёх, три подписи
  статистики, подпись подвала, описание для поисковиков и подзаголовок контактов.
- Решение: панель показывает «Открытый код · 8», а не «Lighthouse 96». Метрика одного
  клиентского сайта на первом экране продаёт один кейс вместо услуги; замер уехал в разбор.
- Решение: с пилюли цен снято число, польза названа словами. «204 в одном файле» — правда,
  но посетитель не знает, чьи это цены и почему один файл лучше десяти.
- Решение: подписи говорят обычными словами — «баллов за скорость» вместо «Lighthouse»,
  «автопроверок» вместо «автотестов». Имя инструмента живёт в разборе, где есть методика.
- Грабли: подвал обещал «собрано без фреймворков», а сайт собран на Astro. Обещание было
  про браузер — там внешних библиотек нет, так теперь и написано.
- Грабли: секция стека звала за примерами «ниже», хотя кейсы стоят выше. Дальше: Ф10 — SEO.

## 2026-08-25 · Ф9 закрыта — форма и приёмник заявок

- Сделано: секция контактов, форма на `POST /api/contact`, приёмник на aiohttp
  в контейнере, страницы ответа `/sent/` и `/not-sent/`, тринадцать проверок в CI.
- Решение владельца: Telegram `@jw_dev_pro` публикуем, почту — нет. Временный ящик
  знает только `.env` на сервере: репозиторий публичный, а личный адрес на витрине
  собирают спам-боты. Корпоративная встанет в контакты вместе с доменом.
- Решение: приёмник отвечает по-разному. Браузеру без JS — редирект на страницу
  ответа, скрипту — JSON: `fetch` глотает редирект молча, и отличить успех от отказа
  было бы нечем. Тексты ответа скрипт берёт с той же страницы, а не хранит у себя.
- Решение: лимит по адресу проверяется первым, до ловушки. Он дешевле всех остальных
  проверок и расходуется на роботах — им поток заявок нужнее, чем человеку.
- Грабли: `hidden` не прячет ничего, у чего есть свой `display` — правило класса идёт
  позже сброса с той же специфичностью. За фазу выстрелило трижды, теперь одно правило
  в `reset.css` вместо четырёх заплаток по компонентам.
- Грабли: панель ответа и скрытые поля нашлись только на снимке экрана — проверка
  `element.hidden` в консоли всё это время отвечала «скрыто». Дальше: ревизия текстов.

## 2026-08-25 · Ф9 — About и семь прямых ответов

- Сделано: секция About со знаком в кольцах и тремя фактами, аккордеон FAQ на ответах
  владельца об условиях — сроки, оплата, код, ежемесячные расходы, чужой проект, старт.
- Решение владельца: города на сайте нет нигде. «Дананг, Вьетнам» из подзаголовка героя
  убран той же правкой — факт был неверен, а покупателю география безразлична.
- Решение владельца: цифры условий публикуются. План запрещал цены, потому что расценок
  не существовало; часовая ставка и вилки поддержки существуют, цена за проект — нет.
- Решение: пункт аккордеона — `<details name>`, а не кнопка. Нативный аккордеон закрывает
  соседей сам, и секция обходится без скрипта — как шторка разделов в §13.
- Грабли: `max-height: 340px` из §16 обрезал бы ответы молча — они длиннее макетных.
  Высоту ведёт `grid-template-rows: 0fr → 1fr`, длительность и easing гайда те же.
- Грабли: строка ответа во всю ширину — 140 знаков. В макете аккордеон делил ряд
  с таблицей пакетов, которой у нас нет. Ограничена 74ch. Дальше: форма и contact-api.

## 2026-08-25 · Долг из Ф2 — док ведёт раздел и действие

- Сделано: строка «сейчас читаете» и контекстное действие дока из §14. Секция сама
  объявляет своё короткое имя и вид действия рядом с `data-acc`, а не в отдельном списке.
- Решение: зонд секции вынесен в `lib/section.js`, на него подписаны и акцент, и док.
  Два замера одного вопроса разъезжаются, и цвет фона начинает спорить с подписью.
- Решение: четыре действия отрисованы на сборке, скрипт переключает видимое. Подставлять
  подписи в браузере значило бы увезти туда словарь.
- Решение: «скопировать почту» из §14 не делали — почты нет, а копировать выдуманный
  адрес хуже, чем не предлагать вовсе. Вернётся в Ф12 вместе с почтой.
- Грабли: `hidden` не прячет `.dock-ctx` — `.btn` объявляет `display` позже reset'а
  и с той же специфичностью. Та же грабля уже стояла у `.jrow` и `.case`.
- Грабли: на ≤640px у кнопок дока не было доступного имени — подписи скрыты, иконка
  `aria-hidden`. Семь секций проверены в браузере через CDP. Дальше: Ф9 — About и форма.

## 2026-08-22 · Фаза 8 — журнал

- Сделано: лента из четырёх записей на главной, страница `/log/` с табами и счётчиками,
  четыре страницы статей, две ленты RSS и строка «последняя запись — сегодня».
- Решение владельца: архив `/log/` — настоящая страница, а не оверлей `.arc` из §15.
  Оверлей там же описан словами «страница /log поверх сайта», а страница уже есть:
  второй список со своими фильтрами пришлось бы держать в синхроне вручную. §15 с датой.
- Решение: подсветка кода — Prism вместо Shiki по умолчанию. Shiki красит инлайновым
  `style=` на каждом токене: 1700 нарушений правила «ноль инлайна» на двух страницах.
  Prism отдаёт классы, цвета им задаёт `note.css` из палитры — четырьмя цветами.
- Решение: «N дней назад» считает браузер, в разметке стоит абсолютная дата. Дата не
  протухает между сборками, а склонения отдаются `Intl.RelativeTimeFormat` — своего
  словаря для «дня / дней / вчера» не нужно.
- Грабли: строка апдейта обрезалась многоточием. У кейса и статьи под заголовком есть
  вторая строка, а у апдейта весь он и есть заголовок — многоточие съедало смысл.
  Нашлось только в браузере, апдейт теперь переносится.
- Грабли: dev-сервер держал старый `astro.config.mjs` и продолжал красить через Shiki
  после правки. Проверять подсветку без перезапуска сервера бесполезно.
- Замечание: контекстное действие дока (§14) так и не сделано с Ф2 — записано в план
  на Ф9. Дальше: фаза 9 — About, FAQ, контакты.

## 2026-08-22 · Фаза 7 — направления, стек, процесс

- Сделано: три карточки направлений без цен, бегущая строка стека, стек в двух состояниях
  и процесс с импульсом на 12 000 мс, пятью узлами и рельсой на узком экране.
- Решение по C4: базовое состояние стека — сгруппированная сетка с подписями групп,
  кольцо надстраивается поверх на десктопе. Группы в кольце уходят в `display: contents`,
  поэтому разметка одна на оба вида и расходиться нечему.
- Решение: чипы разложены вперемешку по группам. Сложенные по порядку, они дают кольцо
  с одним бирюзовым боком и жёлтым напротив.
- Решение: группа «ИИ» — один чип, Claude CLI. Дописать соседей значило бы их придумать.
- Грабли: покадровая раскладка кольца и подсветка шага живут только в кадре анимации,
  а кадров нет ни за экраном, ни в фоновой вкладке — чипы лежали стопкой в центре.
  Обе секции теперь красят себя один раз при включении, кадр только продолжает.
- Замечание владельца: тексты главной взяты по образцу макета и продают не ту услугу.
  Три находки записаны в план отдельным разделом «Ревизия маркетинга и текстов»
  между Ф9 и Ф10. Дальше: фаза 8 — журнал.

## 2026-08-22 · Фаза 6 — кейсы

- Сделано: сетка из семи карточек в трёх состояниях, фильтры со счётчиком, четырнадцать
  страниц `/work/<slug>/`, оверлей на `<dialog>` с History API и картинка шаринга на кейс.
- Решение: карточка — `<article>` со ссылкой, а не `<button>` из гайда. Без JS кнопка
  никуда не ведёт, а ссылка на демо внутри кнопки невалидна. Отступление в §8 с датой.
- Решение владельца (C5): кружки светофора над скриншотом сняты, строка адреса оставлена
  и ужата до 22px. У ботов на её месте имя бота из ссылки.
- Решение: оверлей не собирает разметку из данных, как макет, а забирает её у страницы
  разбора через `fetch`. Словарь кейсов не уезжает в браузер, расходиться нечему.
- Грабли: событие `close` у `<dialog>` в Chrome после `dialog.close()` не приходит вовсе.
  Уборка висела на нём и молча не выполнялась — прокрутка страницы оставалась заблокированной.
  Нашлось только замером в браузере; все пути закрытия сведены в свою функцию.
- Грабли: глобальное правило ритма `section` било по вложенным секциям разбора — по 106px
  внутри каждого абзаца. Ограничено `main > section`. Дальше: фаза 7 — направления, стек, процесс.

## 2026-08-22 · Фаза 5 — контент-слой

- Сделано: схемы коллекций, семь кейсов на двух языках, папка мастер-кадров и настоящий
  `check:content` — пара RU↔EN, счёт предложений в абзацах, уникальность `order`, размер
  и вес снимков по §3. Все четыре пути проверки снимков прогнаны на подложном кадре.
- Решение: русский документ несёт языконезависимые поля, английский переводит только текст.
  Второй копии `link` или `date` не существует — разъезжаться нечему. Имя файла и есть слаг
  адреса: отдельное поле однажды разошлось бы с именем.
- Решение: факты собраны из README, `prices.json` и деревьев репозиториев, а не из макета.
  Макет ошибся шесть раз: aiogram у автопостера, Nginx у самуйского сайта, четыре потока
  вместо пяти у дананга, зелёный акцент вне палитры, «сайт с нуля» у клиники, 24 страницы.
- Решение: `zod` импортируется из пакета напрямую и объявлен зависимостью. Реэкспорт `z`
  из `astro:content` в Astro 7 устарел и давал 52 подсказки; `yaml` был транзитивным
  и мог уехать с чужим апдейтом.
- Грабли: русский текст во front-matter ломает YAML — `: ` внутри фразы читается как ключ.
  Все длинные поля переведены на блочные скаляры `>-`, кавычек в контенте владельца не будет.
- Грабли: замер Lighthouse не подтвердил цифру макета. `psy-krasnogor.pro` даёт 87, а не 96:
  203 КБ шрифтов из 291 КБ страницы объявлены без `unicode-range`. Панель героя переведена
  на `vn.neva.beauty`, где 96 измерены. Дальше: фаза 6 — кейсы.

---

Более ранние записи — в [`docs/journal-archive/2026-08.md`](docs/journal-archive/2026-08.md).

## 2026-08-22 · Замер веса после ухода с Tilda

- Сделано: посчитан вес страниц по сохранённому экспорту `doctor.cosmetolog.pro` — предшественника
  на Tilda. Девятнадцать одинаковых страниц, плюс отдельно `vn.neva.beauty` против того же «до».
- Решение: цифра на главной заменена. «−51…−60% веса страниц» замер не подтвердил: легче стал
  код (медиана −70% сырого, −75% по сети), а страница целиком у части разделов тяжелее —
  на новых страницах появились фотографии, которых на Tilda не было вовсе.
- Грабли методики: полифилл Tilda на 181 КБ помечен `nomodule`, современный браузер его не качает;
  иконка сайта весила 2,3 МБ и кешируется на весь визит; из набора `srcset` браузер берёт один файл.
  Без этих трёх поправок «до» завышалось вдвое, а «после» — впятеро.
- Дальше: фаза 5 — контент-слой. У кейса vn открыт вопрос: он написан как «сайт с нуля»,
  а тильдовская версия, по словам владельца, была.

## 2026-08-22 · Фаза 4 — герой и статистика

- Сделано: двухколоночный герой, сцена из трёх окон с каустиками и HUD, параллакс от курсора
  и прокрутки, четыре счётчика. Заглушка главной удалена вместе со стилями и ключами.
- Решение C3: отступ секции срезан до `clamp(64px, 7vw, 112px)`. Измерено на живой странице:
  296px между блоками на окне 784px — больше трети экрана, и занять его нечем. §4 гайда правлен.
- Решение: глубина `--tz` уехала из `data-tz` в CSS (без JS сцена схлопывалась в плоскую
  аппликацию), своего цикла у параллакса нет (подписан на сглаженный курсор из `pointer.js`,
  иначе слои дрожат), а якорь сети висит на слое дрейфа: `getBoundingClientRect()`
  не видит трансформ ребёнка, и нить оставалась бы там, где окно было бы без покачивания.
- Решение: `99.9%` аптайма из макета выкинуто — никто не мерил. На его месте «Цены / 204
  в одном файле». Числа: 7 проектов, LH 94–98, −51…−60% веса, 85 автотестов — из репозиториев.
- Грабли счётчика: диапазон стартует со своей нижней границы (`data-from`), иначе «94–98»
  по дороге показывает «94–37»; отматывать назад можно только то, чего не видно, — карточка
  проявляется раньше, чем идёт счёт, и цифра прыгала в ноль на глазах.
- Грабли: макет считан на английских строках. Русские длиннее, и в полосе 900–1050px HUD
  ложился на кнопку и на бровь — левые пилюли прижаты к кромке сцены. Дальше: фаза 5.

## 2026-08-22 · Свой шрифт и словесный знак

- Сделано: Onest переменный в `public/fonts/` (кириллица 14 КБ + латиница 32 КБ, OFL),
  `fonts.css` с `unicode-range`, предзагрузка подмножества своего языка; словесный знак
  `jw-dev.pro` в шапке. Ответы владельца на ревизию: C1 — свой шрифт, C2 — акцентов
  остаётся пять, C6 — имя из домена.
- Решение: трекинг не трогали. Onest держит −0.042em на «Жюри съело шхуну — ЖЫЮЪ»
  без слипания, так что числа гайда теперь описывают конкретную гарнитуру, а не SF Pro Display.
- Решение: моно остаётся системным — он живёт на подписях 10–12px, где разница гарнитур
  не читается, а второй файл стоил бы дороже пользы.
- Решение: `aria-label` со ссылки-логотипа снят, доступным именем стал сам знак. Подпись,
  расходящаяся с видимым текстом, ломает голосовое управление. Ключ `a11y.home` удалён.
- Грабли: Astro, в отличие от JSX, сохраняет перенос строки как пробел — знак распадался
  на «jw-dev .pro». Разметка обязана остаться в одну строку, в коде стоит предупреждение.
- Грабли: бюджет веса собирал ассеты по ссылкам из разметки и шрифты не видел вовсе —
  подмножество выбирает CSS через `unicode-range`. Теперь `.woff2` считаются пессимистично.

## 2026-08-22 · Ревизия дизайна перед фазой 4

- Сделано: индиго расщеплён на `--acc` (заливки) и `--acc-ink` (текст, обводки, линии);
  подвал прижат к низу окна; пустое состояние снято с `.mono`; ховер убран под
  `@media (hover: hover)`; при reduced-motion граф рисует статичный кадр вместо исчезновения.
- Решение владельца: `#4F46E5` даёт 3,16:1 и не проходит AA. Правило 7 (WCAG) против §2 гайда
  разрешено в пользу доступности; `#818CF8` — тот же оттенок, 6,66:1. Гайд правлен вслед за кодом.
- Решение: верхняя ступень `#veil` поднята с 30% до 42%. На стыке пятен `.b1` и `.b6` подпись
  цветом `--ink3` давала 4,78:1 при пороге 4,5 — запас в четверть пункта, теперь 5,08.
- Грабли: перенос ховера в конец файла меняет порядок каскада. `.fbtn:hover` и
  `.fbtn[aria-pressed]` имеют равную специфичность — активный таб перекрашивался, спасло `:not()`.
- Дальше: фаза 4 — герой и статистика. Из ревизии закрыты C1, C2 и C6; C3 (ритм секций),
  C4 (кольцо стека) и C5 (хром карточки) записаны условиями в фазы 4, 7 и 6 — там их и решаем.

---

## 2026-08-22 · Фаза 3 — фон и движение

- Сделано: аврора, вуаль, свет за курсором, канвас-граф с якорями `data-nx`, импульсами
  и демпфированием, механика акцента секции, появление `.rv`. Заодно поставлен
  `@astrojs/check`: без него ворота `check:astro` молча проходили, не проверив ничего.
- Решение: пары цветов акцента живут в `accent.css`, скрипт переносит на `<html>` только имя.
  Значений цвета в JS нет вовсе — граф снимает палитру с `:root` при старте.
- Решение: у пятен ауроры нет `filter: blur(100px)` из §1 — в макете его тоже нет. Мягкость
  даёт сам градиент, а размытие во весь экран отняло бы у графа кадры.
- Грабли: `inset: 0` не растягивает `<canvas>`. Замещаемому элементу при `width: auto`
  браузер оставляет размер растра и правый упор игнорирует; нужны проценты.
- Грабли: предохранитель `.rv` из макета через 1.2 с гасит анимацию на всей остальной
  странице. Снимаем его первым же ответом наблюдателя и не заводим в фоновой вкладке.
- Дальше: фаза 4 — герой и статистика.

## 2026-08-21 · Фаза 2 — каркас страницы

- Сделано: маршруты `/log/`, `/privacy/` и 404 на обоих языках, шапка со знаком
  и переключателем языка, шторка разделов, подвал, док с кольцом прогресса.
- Решение: меню — настоящий `<details>`, а не кнопка с обработчиком: без JS оно обязано
  открываться. Шторка начинается под шапкой, чтобы бургер оставался виден и закрывал её.
- Решение: в подвале только GitHub. Почты `info@jw-dev.pro` и Telegram-хендла ещё нет,
  а выдуманный контакт хуже отсутствующего.
- Грабли: вложенный `backdrop-filter` ничего не размывает — элемент с этим свойством
  создаёт backdrop root. Пока меню открыто, шапка отдаёт блюр шторке.
- Грабли: Astro вшивал модули в разметку. Порог снимается через
  `vite.build.assetsInlineLimit`; ключа `build.assetsInlineLimit` у Astro нет.
- Дальше: фаза 3 — фон и движение.

## 2026-08-21 · Фаза 1 — дизайн-система в код

- Сделано: токены, типографика, стекло, `.irid`, кнопки и пилюли разложены по файлам
  в `src/styles/`; вход один — `main.css`, Vite склеивает импорты в один файл на сборке.
- Решение: короткие имена из гайда (`.g`, `.gd`, `.rv`, `.btn.p`) не переименовывал.
  Гайд и макет — справочник по значениям, свои имена сделали бы его непригодным.
- Решение: инлайновый `--cc` заменён шестью классами `.tone-*`, а не `.case--*`:
  тот же акцент понадобится чипам метрик и чипам кольца стека, не только карточке кейса.
- Грабли: `.irid` заливал карточку целиком вместо волосяной рамки. `-webkit-mask` —
  псевдоним `mask` и сбрасывает `mask-composite`, если стоит после него. Порядок как в макете.
- Добавлено: ворота `check:css` — брейкпоинты, easing, радиус блюра и палитра сверяются
  с гайдом на каждой сборке, чтобы числа не разъезжались по компонентам.
- Дальше: фаза 2 — каркас страницы.

## 2026-08-21 · Фаза 0 — репозиторий и рельсы

- Сделано: Astro 7.2.4, структура папок, i18n-каркас (EN на `/`, RU на `/ru/`), знак `>jw_`,
  заглушка на обоих языках, линтеры, шесть проверок в `npm run check`, Nginx с CSP, CI.
- Решение: стили только глобальными файлами, без `<style>` в компонентах — гайд написан
  как глобальный CSS, и из HTML уходят служебные `data-astro-cid-*`.
- Решение: `build.inlineStylesheets: 'never'` и `compressHTML: true`. Первое — иначе Astro
  вшивает мелкие стили в разметку и ломает CSP. Второе — в Astro 7 по умолчанию `'jsx'`,
  где пробел между строчными элементами съедается.
- Грабли: `eslint-plugin-jsx-a11y` не поддерживает ESLint 10. Ломать дерево
  `--legacy-peer-deps` не стал, доступность проверяет `html-validate:a11y` на собранном HTML.
- Грабли: линтер нашёл мёртвый `|| '/'` в `localizePath` — шаблонная строка всегда истинна.
- Заблокировано: домен не куплен, доступа к серверу нет. Выкат ждёт и то и другое.
- Дальше: фаза 1 — дизайн-система в код.
