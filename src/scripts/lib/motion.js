/**
 * Запрос «меньше движения» из системных настроек.
 *
 * Экспортируется сам MediaQueryList, а не его значение: список нужен ещё
 * и как источник события change — настройку меняют, не перезагружая
 * страницу, и фазовые эффекты обязаны это заметить.
 */
export const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
