/**
 * Постоянные сайта. Здесь только то, что подтверждено владельцем и работает:
 * репозитории открыты, бот отвечает, личный Telegram существует, почта
 * принимает письма.
 *
 * Почта появилась 04.09.2026, и в тот же день владелец решил, что она у сайта
 * единственная: `info@` и показывается посетителю, и принимает заявки из формы.
 * Личный ящик выведен из работы сайта совсем — его нет ни в разметке,
 * ни в коде, ни в настройках приёмника.
 *
 * У приёмника этот же адрес продублирован литералом (`server/contact-api/config.py`):
 * питон не умеет читать TypeScript, а проверки на расхождение нет. Правишь
 * здесь — правь и там.
 *
 * Адрес проверен до публикации, а не со слов: `dig MX jw-dev.pro` отдаёт
 * `mx1/mx2.titan.email`, SPF и DKIM на месте. Показывать ящик, который
 * не принимает почту, — та же выдуманная цифра, только дороже.
 */
export const SITE = {
  name: 'jw-dev.pro',
  /** Корпоративный ящик. Показывается на сайте — для того и заведён. */
  email: 'info@jw-dev.pro',
  github: 'https://github.com/jw-git-hub',
  bot: 'https://t.me/jw_social_Downloader_bot',
  telegram: 'https://t.me/jw_dev_pro',
  /** Имя пользователя для видимой подписи ссылки — адрес выше собирается из него. */
  telegramName: '@jw_dev_pro',
} as const;

/** Год для строки копирайта берётся на сборке, а не пишется руками. */
export const BUILD_YEAR = new Date().getFullYear();
