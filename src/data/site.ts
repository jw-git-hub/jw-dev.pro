/**
 * Постоянные сайта. Здесь только то, что подтверждено владельцем и работает:
 * репозитории открыты, бот отвечает, личный Telegram существует.
 *
 * Почты здесь нет намеренно. Временный адрес владельца знает только сервер
 * (`MAIL_TO` в `.env`) — на сайте он не показывается нигде: репозиторий
 * публичный, а личный ящик на витрине собирают спам-боты. Корпоративная
 * `info@jw-dev.pro` встанет сюда в Ф12, когда домен будет куплен.
 */
export const SITE = {
  name: 'jw-dev.pro',
  github: 'https://github.com/jw-git-hub',
  bot: 'https://t.me/jw_social_Downloader_bot',
  telegram: 'https://t.me/jw_dev_pro',
  /** Имя пользователя для видимой подписи ссылки — адрес выше собирается из него. */
  telegramName: '@jw_dev_pro',
} as const;

/** Год для строки копирайта берётся на сборке, а не пишется руками. */
export const BUILD_YEAR = new Date().getFullYear();
