/**
 * Четыре числа главной — DESIGN-GUIDE §8, план Ф4.
 *
 * Железное правило проекта: цифра либо измерена, либо её нет. Источник
 * каждой указан рядом; выдуманного процента здесь не появится никогда,
 * даже если блок будет выглядеть пустее.
 *
 * `from` — значение, с которого начинает счётчик. У диапазона это его
 * нижняя граница: считать «94–98» от нуля значит показать посетителю
 * несуществующие «94–37».
 *
 * Диапазоны записаны по-разному намеренно: у положительного — тире («94–98»),
 * у отрицательного — многоточие («−51…−60%»), потому что тире после минуса
 * читается как вычитание. Это не разнобой, приводить к одному виду не надо.
 */
import type { Accent } from './accent';

export interface Stat {
  /** Ключ подписи в src/i18n. */
  key: string;
  /** Конечное значение счётчика. */
  value: number;
  /** Начало отсчёта; по умолчанию ноль. */
  from?: number;
  /** Неизменяемая часть перед числом и после него. */
  prefix?: string;
  suffix?: string;
  /** Цвет пятна в углу карточки. */
  accent: Accent;
}

export const STATS: Stat[] = [
  /*
   * Репозитории jw-git-hub: семь проектов, восьмой репозиторий — этот сайт.
   * Подпись — «в работе», а не «в проде»: neva-beauty-bot по своему TODO.md
   * живёт на тестовом окружении, боевой запуск у него впереди.
   */
  { key: 'stat.projects', value: 7, accent: 'cyan' },

  /*
   * Диапазон из README vn.neva.beauty, подтверждён своим замером 22.08.2026:
   * Lighthouse 12.8.2, мобильный профиль, три прогона — 96, 98, 95.
   * Подпись называет сайт намеренно. Общей цифры «по проектам» здесь быть
   * не может: тем же замером psy-krasnogor.pro дал Performance 87.
   *
   * Имя инструмента с подписи снято при ревизии текстов 25.08.2026: «баллы
   * за скорость» понимает и тот, кто про Lighthouse не слышал. Версия
   * инструмента и методика замера остались в разборе кейса.
   */
  { key: 'stat.speed', value: 98, from: 94, prefix: '94–', accent: 'violet' },

  /**
   * Замер 22.08.2026 по сохранённому экспорту предшественника на Tilda
   * (doctor.cosmetolog.pro): девятнадцать одинаковых страниц, код страницы
   * (разметка + стили + скрипты, без шрифтов и фотографий) — медиана −70%
   * сырого веса и −75% по сети. У vn.neva.beauty против того же «до» — −72%.
   * Берём меньшее из двух.
   */
  { key: 'stat.weight', value: 70, prefix: '−', suffix: '%', accent: 'rose' },

  /*
   * th.neva.beauty: 85 pytest-тестов читают источник цен и проверяют
   * отрисованное — цифру, раздел, описание, валюту и иерархию заголовков.
   * Отсюда «цены и тексты» в подписи, а не общее слово «контент».
   */
  { key: 'stat.tests', value: 85, accent: 'amber' },
];
