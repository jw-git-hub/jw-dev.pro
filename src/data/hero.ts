/**
 * Данные сцены героя — DESIGN-GUIDE §10.
 *
 * Здесь то же правило, что и у чисел статистики: только подтверждённое.
 * Пилюли висят в воздухе вокруг окон и читаются раньше подписей, поэтому
 * источник каждой цифры указан рядом.
 *
 * Ревизия текстов 25.08.2026: сцена перестала показывать метрику одного
 * клиентского сайта. На первом экране стоят условия работы и открытый код —
 * то, что покупатель ищет до кейсов; цифры отдельных проектов живут
 * в их разборах.
 *
 * `place` — класс позиции в `hero.css`: он задаёт угол сцены и глубину `--tz`.
 * Позиция лежит рядом с самой пилюлей, а не выводится из порядка списка:
 * пятая пилюля в таком списке молча осталась бы без позиционного класса.
 *
 * `amp` — размах параллакса (§10), его читает `parallax.js`.
 */
export interface HudPill {
  /** Общий ключ пары: `<key>.k` — подпись, `<key>.v` — значение. */
  key: string;
  place: 'hudA' | 'hudB' | 'hudC' | 'hudD';
  amp: number;
}

export const HUD_PILLS: HudPill[] = [
  // Бот отвечает прямо сейчас: t.me/jw_social_Downloader_bot, проверено вручную.
  { key: 'hud.bot', place: 'hudA', amp: 12 },

  // Стек ботов по их README: Python, aiogram 3, Docker.
  { key: 'hud.stack', place: 'hudB', amp: 11 },

  // Условие владельца из ответа FAQ: репозиторий переносится на аккаунт заказчика.
  { key: 'hud.code', place: 'hudC', amp: 10 },

  /*
   * th.neva.beauty: 204 цены сведены в один источник правды. Само число
   * с пилюли снято — вне кейса оно не сообщает посетителю ничего, а польза
   * от одного источника называется словами.
   */
  { key: 'hud.prices', place: 'hudD', amp: 12 },
];

/*
 * github.com/jw-git-hub: восемь репозиториев открыты — семь проектов
 * и этот сайт.
 *
 * Раньше панель показывала Lighthouse 96 сайта клиники. Главный экран
 * продавал этим один чужой сайт вместо услуги, и цифра уехала в разбор
 * кейса vn.neva.beauty, где у неё есть методика замера.
 */
export const OPEN_REPOS = '8';
