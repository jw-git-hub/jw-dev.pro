# Копия этого файла лежит на сервере как .env и в репозиторий не попадает.
# Заполняется в фазе 9 вместе с приёмником формы.

# Куда падают заявки с формы
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Дубль заявки на почту
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_TO=info@jw-dev.pro

# Защита от спама: сколько заявок с одного адреса за окно
RATE_LIMIT_PER_HOUR=5
